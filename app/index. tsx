import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

export default function Index() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Success! Day 1 complete. ✅</Text>
      <Text style={styles.subText}>My app is finally working, bro!</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { 
    flex: 1, 
    justifyContent: 'center', 
    alignItems: 'center', 
    backgroundColor: '#fff' 
  },
  text: { 
    fontSize: 24, 
    fontWeight: 'bold', 
    color: '#007AFF' 
  },
  subText: {
    fontSize: 16,
    marginTop: 10,
    color: '#333'
  }
});